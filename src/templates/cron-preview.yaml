###
# CRON Preview
# ------------
# This CRON workflow will look for old deployments (> 30 days) and delete them. You can configure the
# CRON Preview in the launchpad.yaml for a more aggressive time internval.
#
# IMPORTANT: Managed by LaunchPad DO NOT EDIT!
###

on:
  pull_request:
    branches:
      - main
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@master
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true


# label == Preview with timestamp

#todos:
#  - On merge destroy existing
#  - handle setting up DB and injecting